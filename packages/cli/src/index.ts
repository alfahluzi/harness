/**
 * @puna/cli — plugin authoring CLI.
 *
 * Commands (Fase 7):
 *   puna plugin create <name>   scaffold from templates/plugin/<archetype>
 *   puna plugin dev             watch + serve UI bundles
 *   puna plugin build           production Bun.build bundle
 */
export const CLI_VERSION = '0.1.0';