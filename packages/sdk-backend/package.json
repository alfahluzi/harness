{
  "name": "@puna/sdk-backend",
  "version": "0.1.0",
  "description": "Plugin SDK — backend PluginHost, scanner, hook bridge, Hono routes.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && bun run build:js && bun run build:types",
    "build:js": "bun build ./src/index.ts --outdir dist --target node --format esm --external @puna/sdk-shared --external zod --external hono",
    "build:types": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@puna/sdk-shared": "workspace:*",
    "zod": "^3.23.0"
  },
  "peerDependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "~6.0.2"
  },
  "keywords": [
    "puna",
    "plugin-sdk",
    "backend"
  ],
  "license": "MIT"
}